# R / RStudio
.Rproj.user/
.RData
.Rhistory
.Ruserdata

# Rendered output (regenerate from source)
*.html
*_files/
*_output/
Rplots.pdf

# renv library (lock file is tracked; library is restored from it)
renv/library/
renv/staging/
renv/sandbox/
renv/cellar/
renv/local/
renv/python/

# OS
Thumbs.db
.DS_Store

# Large/binary files that shouldn't be in git
*.png
*.pdf
*.pptx
*.docx

# Raw subject-level data (PHI: consent forms, clearance forms, HR/HRV exports).
# IRB-approved anonymized analytic data lives in MRP/MRP.xlsx (tracked).
DATA/

# Internal session/operational notes (not for public repo):
#   - LOOSE_ENDS.md: inter-session signals (HR data scoping, etc.)
#   - journal_target_audit_*.md: journal targeting strategy
#   - manuscript_revision_plan.md: contains verbatim reviewer comments (confidential)
LOOSE_ENDS.md
journal_target_audit_*.md
manuscript_revision_plan.md
POLISH_PUNCHLIST.md
